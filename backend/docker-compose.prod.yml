services:
  backend:
    build:
      context: ..
      dockerfile: backend/Dockerfile
    env_file: .env
    volumes:
      - ./data:/app/data
    ports:
      - "8000:8000"
    restart: unless-stopped
